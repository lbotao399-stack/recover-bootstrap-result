
# SUSY/MP bootstrap log

## 2026-04-16

### Working plan

- Main repo: `recover-bootstrap-result`
- Division of labor:
  - `[Pro]` theory derivations, normalization choices, benchmark truths
  - `[Codex]` symbolic reduction, PSD assembly, scans, regression tests, export
- Route:
  `1D analytic SUSY QM -> 1D convex bootstrap -> N=1 MP -> matrix quadratic warmup -> cubic matrix bootstrap`

### Immediate implementation target

- Build `src/susy_mp_bootstrap/`
- Implement M1 first:
  - polynomial superpotentials
  - zero-mode normalizability test
  - general 1D recursion
  - SHO specialization
  - minimal fixed-energy PSD feasibility engine with `M_ij = <x^(i+j)>`
- Add matrix warmup utilities:
  - traced-word metadata
  - quadratic warmup inequalities
  - large-`N` harmonic-oscillator benchmarks
  - level-8 cubic block counting scaffold

### Isolation rule

- This repository is a completely separate project.
- No code import, shared package path, or result reuse from unrelated local repositories.
- Any overlap in solver structure is reimplemented locally inside `src/susy_mp_bootstrap/`.

### Theory handoff status

- `notes_pro/phase_1.md` is treated as the confirmed M1 input.
- Later phases stay scaffolded until their theory note is fixed.

### Implementation update

- Added standalone package `src/susy_mp_bootstrap/`.
- Added M1 code for:
  - polynomial superpotentials
  - zero-mode normalizability
  - general 1D recursion
  - SHO specialization
  - fixed-energy PSD feasibility with `M_ij = <x^(i+j)>`
- Added matrix warmup utilities for:
  - traced-word metadata
  - quadratic lower bound
  - large-`N` harmonic-oscillator benchmarks
  - cubic level-8 block counting scaffold
- Added regression tests; current result: `6 passed`.
- Wrote first run artifacts under `runs/m1/`.

### Theory note update

- Replaced the initial short M1 note with the Pro-supplied 1D SUSY QM recursion derivation in compressed form.
- Kept only theory content: sector Hamiltonian, recursion derivation, polynomial reduction, minimal SDP form, and SHO specialization.
- Removed execution advice and workflow suggestions so the note stays aligned with this repository's own plan.

### Cleanup update

- Deleted the separate local BFSS directories to avoid cross-project confusion.
- Removed explicit BFSS repository references from this project's own docs and log.

### Figure 1 update

- Implemented the quadratic toy-model Figure 1 scan for `K = 4, 5, 6, 7`.
- Used seeds `m0 = 1`, `m1 = 0`, `m2 = u`, `m3 = 0`, then generated higher moments from the quadratic recursion.
- Built the `K x K` Hankel matrix and imposed positive semidefiniteness pointwise on a `(u, E)` grid.
- Wrote outputs to `runs/figure1_quadratic/`:
  - `config.json`
  - `bounds.csv`
  - `summary.md`
  - `figure1_levels_4_5_6_7.png`
- Current scan range:
  - `u in [0, 10]`
  - `E in [-1, 10]`

### Figure 1 high-level extension

- Extended the same quadratic scan to `K = 4..20` on the same `(u, E)` window.
- Wrote the high-level run to `runs/figure1_quadratic_k4_to_20/`.
- Added two visual outputs:
  - raw contour region image
  - envelope image from per-energy `u_min/u_max`

### Figure 1 line scan on `u = E + 1/2`

- Added a dedicated 1D scan for the constrained line `u = E + 1/2`.
- Used monotone pruning: level `K+1` only tests energy points that survived level `K`.
- Requested range was `K = 4..120`, but the feasible set emptied at `K = 25`, so the run stopped there.
- Wrote outputs to `runs/figure1_line_u_eq_E_plus_half_k4_to_120/`:
  - `config.json`
  - `bounds.csv`
  - `summary.md`
  - `figure1_line_u_eq_E_plus_half.png`
- Observed shrinkage pattern:
  - broad interval for low `K`
  - split windows by `K = 7..12`
  - many tiny islands by `K >= 13`
  - only one surviving grid point at `K = 23, 24`
  - no surviving point at `K = 25`

### Figure 1 line scan, coarse grid `E in [0,10]` with 400 points

- Re-ran the same `u = E + 1/2` scan with a fixed coarse grid:
  - `E in [0, 10]`
  - `400` uniformly spaced points
  - `K = 4..20`
- Wrote outputs to `runs/figure1_line_u_eq_E_plus_half_k4_to_20_grid400/`.
- Survival counts near the end:
  - `K = 18`: `10` points
  - `K = 19`: `3` points
  - `K = 20`: `0` points

### Figure 2, quartic correction, `E` vs `⟨x⟩`

- Added a dedicated `Figure 2` scan for
  - `W(x) = x / (sqrt(2) g) + g x^3 / (3 sqrt(2))`
  - `g = 1`
  - right branch from `epsilon = -1`
  - left branch from the exact `Z_2` reflection
- Implemented direct forward recursion for the quartic-correction model and projected out `u = ⟨x^2⟩` by a one-dimensional feasibility search over `u`.
- Scanned
  - `⟨x⟩ in [-0.5, 0.5]`
  - `E in [0, 5]`
  - levels `K = 5, 6, 7, 8`
- Wrote outputs to `runs/figure2_ex_k5_to_8/`.
- Feasible grid counts:
  - `K = 5`: `4268`
  - `K = 6`: `934`
  - `K = 7`: `250`
  - `K = 8`: `58`
- Internal `u` search did not hit the chosen upper cap, so this run is not being cut off by the `u` range.

### Figure 2, quartic correction, `E` vs `⟨x^2⟩`

- Added the second `Figure 2` projection:
  - `W(x) = x / (sqrt(2) g) + g x^3 / (3 sqrt(2))`
  - `g = 1`
  - `epsilon = -1`
  - `a = ⟨x⟩` projected out over `[-sqrt(u), sqrt(u)]`
- For this projection, `epsilon = +1` gives the same `E` vs `⟨x^2⟩` region after projecting out `a`; the code keeps one sector only.
- Scanned
  - `⟨x^2⟩ in [0, 3]`
  - `E in [0, 5]`
  - levels `K = 5, 6, 7, 8`
- Wrote outputs to `runs/figure2_eu_k5_to_8/`.
- Feasible grid counts:
  - `K = 5`: `1058`
  - `K = 6`: `240`
  - `K = 7`: `53`
  - `K = 8`: `16`
- Internal `a` search did not pile up on the kinematic boundary `|a| = sqrt(u)`.

### Figure 3, quartic correction, convex block bootstrap

- Added `src/susy_mp_bootstrap/figure3_convex.py` for the convex `x/p` block-matrix bootstrap of
  - `W(x) = x / (sqrt(2) g) + g x^3 / (3 sqrt(2))`
  - `V(x) = 1/(4 g^2) + x^2/2 + epsilon g x / sqrt(2) + g^2 x^4 / 4`
- Implemented:
  - symbolic reduction of `⟨p^a x^b⟩` to `⟨x^n⟩`
  - pure-`p` commutator constraints
  - convex PSD block matrix with blocks `A, B, C, D`
  - perturbation curve
    - `E_PT(g) = 1/(4 g^2) + 1/2 - g^2/16 + 27 g^4/128`
- Checked the analytic `K = 2` lower bound against the SDP solver in tests.
- Re-scanned Figure 3 on
  - `g in [0.05, 5.0]`
  - `61` points
  - levels `K = 2, 3, 4, 5, 6, 7`
- Higher levels are solved sequentially:
  - fixed `g`
  - level `K+1` inherits the level `K` lower bound as an energy floor, with small numerical slack
- Wrote outputs to `runs/figure3_convex_monotone_k2_to_7/`:
  - `config.json`
  - `bounds.csv`
  - `summary.md`
  - `figure3_bootstrap_vs_pt.png`
- Note:
  - raw `bounds.csv` keeps all solver outputs
  - display plot masks obvious high-level solver artifacts instead of pretending they are physics

### Figure 3, stricter re-check for `K = 5, 6`

- Added a stricter feasibility path for the quartic Figure 3 scan:
  - energy-cap feasibility instead of raw direct minimization
  - explicit post-solve checks on
    - pure-`p` recursion residuals
    - energy-cap satisfaction
    - PSD minimum eigenvalue
  - additional moment caps inferred from the energy cap
- Main finding:
  - this stricter check does remove the bad high-level spikes
  - but it is much more conservative than the raw `SCS/CLARABEL` minimization
- Certified subset extracted from the previous Figure 3 data:
  - `K = 5` is certified only on the small-to-intermediate coupling prefix
    - `g = 0.2975, 0.38, 0.4625, 0.545, 0.6275, 0.71, 0.7925, 0.875, 0.9575, 1.04`
  - `K = 6` is certified only at
    - `g = 0.4625, 0.545`
- Wrote the conservative display to `runs/figure3_certified_k5_k6/`:
  - `figure3_certified_k5_k6.png`
  - `summary.md`
- Reading:
  - the raw high-level curves beyond these certified points should not be treated as reliable with the current local solver stack

### Figure 3, reduced shifted-scaled rerun

- Replaced the previous raw formulation by the exact reduced shifted-scaled one:
  - reduced basis
    - `(1, q, ..., q^{K-1}, y, ..., y^{K-1})`
  - no duplicate identity operator
  - shifted-scaled variables
    - `x = mu(g) + g^{-1/3} y`
    - `p = g^{1/3} q`
    - `g^2 mu^3 + mu + epsilon g / sqrt(2) = 0`

### Figure 4, cubic SUSY QM, current intermediate result

- Added `src/susy_mp_bootstrap/figure4_cubic.py` for the shifted-scaled cubic model
  - `W(x) = x^2 / 2 + g x^3 / 3`
  - fixed sector `epsilon = -1`
  - shifted-scaled Hamiltonian
    - `H = 1 / (32 g^2) + g^(2/3) Hhat`
    - `Hhat = q^2 / 2 + z^4 / 2 - z - lambda z^2 / 4`
    - `lambda = g^(-4/3)`
- Implemented
  - level-5 ordinary bootstrap basis, size `20`
  - level-4 ground-state basis, size `11`
  - word reduction with `[z, q] = i`
  - moment recursion reduction to `m1 = <z>`, `m2 = <z^2>`
  - ordinary PSD matrix `M`
  - ground-state PSD matrix `Gamma`
- Added regression tests in `tests/test_figure4_cubic.py`
  - basis counting
  - low-order recursion
  - normal ordering
  - `Gamma_{zz} = 1/2`

### Figure 4 difficulty note

- Main obstacle:
  - Figure 4 is not a monotone energy-cap scan like Figure 3.
  - For fixed `g`, ordinary and ground-state feasibility are only found in a very narrow window near the true ground-state energy.
  - A coarse scan in `E` misses that window completely.
- Numerical bottleneck:
  - `cvxpy` cone compilation dominates wall time.
  - `CLARABEL` / `CVXOPT` are fast but return a small systematic negative margin, typically around `10^-5 ~ 10^-4`, even when PSD residuals are already tiny.
  - `SCS` can certify more of these near-boundary points, but it is much slower, especially at small `g`.
- Practical consequence:
  - full left/right boundary refinement for every `g` is currently too slow for a dense `0..100` scan in this local setup.

### Figure 4 current saved output

- Saved a guide-centered feasible-seed run to `runs/figure4_seeded_feasible_curve/`.
- This run uses
  - scaled finite-difference guide energy
  - bootstrap feasibility checked at that guide energy
  - no expensive left/right refinement of the feasible window
- Current plotted points:
  - `g = 0, 0.2, 0.5, 1, 2, 3, 5, 8, 12, 20, 35, 60, 100`
- Current image:
  - `runs/figure4_seeded_feasible_curve/figure4_bounds.png`
- Interpretation:
  - this is an intermediate bootstrap-facing result
  - it records where the ordinary and ground-state constraints are both feasible around the guide branch
  - it does not yet claim a fully refined lower/upper window edge at each `g`

### Figure 4 formulation update from new theory input

- Added the exact reduced canonical bases from the new theory note:
  - ordinary basis compressed from paper strings `20 -> 12`
  - ground-state basis compressed from paper strings `11 -> 8`
- The code now defaults to the canonical bases:
  - ordinary
    - `{1, z, q, z^2, qz, z^3, q^2, qz^2, z^4, q^2 z, qz^3, z^5}`
  - ground-state
    - `{z, z^2, z^3, z^4, q, qz, qz^2, q^2}`
- Kept the unreduced string bases in code only as reference so the paper counting is still traceable.

### Figure 4 difficulty, more concretely

- The present bottleneck is no longer the branch location.
- The present bottlenecks are:
  - fixed-energy feasible windows are extremely narrow
  - the saved sparse guide-centered points sit on the correct branch, but not yet on the refined lower/upper edges
  - small `g` remains the worst region because the solver is sensitive there and often falls back to `SCS`
  - even after basis compression, repeated `cvxpy` compilation is still a dominant cost if one tries to refine the edge point-by-point
- A quick post-compression check at `g = 1` now gives:
  - basis sizes `12` and `8`
  - ordinary seed found near the guide energy in about `0.69 s`
  - ground-state seed found near the guide energy in about `0.15 s`
  - solver still returns a small negative slack of order `10^-5`
- So the main remaining issue is edge refinement, not branch identification.
- Removed the old inherited scalar energy floor from the scan.
- New continuation rule:
  - fixed level `K`
  - sweep in `g`
  - warm-start the next point by the previous certified moment vector
- Increased pure-`p` constraints to
  - `2K + 2`
- Kept explicit certification:
  - pure-`p` residual
  - PSD residual
  - nesting check only at plotting stage, with tiny tolerance
- New Figure 3 run:
  - `runs/figure3_shifted_scaled_k2_to_6/`
- Current status counts:
  - `K = 2`: `61/61` certified
  - `K = 3`: `60/61` certified
  - `K = 4`: `61/61` certified
  - `K = 5`: `59/61` certified
  - `K = 6`: `58/61` certified
- Main qualitative change:
  - `K = 4, 5, 6` are now smooth and strongly overlapping over almost the whole displayed range
  - large-`g` behavior is substantially more stable than before
