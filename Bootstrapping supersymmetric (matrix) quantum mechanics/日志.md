# SUSY/MP bootstrap 日志

## 最新未解决困难（Figure 6，优先关注）

- 现状：
  - Figure 6 已切到 `eta = ln(E / E_inst)` 纵轴，并完成层级扫描接口：`level 7 -> 8 -> 9 -> 10`。
  - 已有产物：
    - `runs/figure6_level7/`
    - `runs/figure6_hierarchy_eta_levels7_to_10_sparse/figure6_hierarchy_eta.png`
  - `level 10` 在稀疏 6 点集上开始显出窗口收缩；`level 7/8/9` 在当前容差下几乎重合。
- 难点：
  - very-small-`g` 端依然被 `eta` 扫描窗口截断：当前很多 lower 点卡在 `eta=-10`，部分 upper 点卡在 `eta=10`。
  - `level 10` 的主要瓶颈已经不是 solver 边界，而是 `CVXPY` 对大块表达式的编译开销。
  - 在双精度和当前容差下，提高到 `level 8/9` 后并没有稳定分辨出比 `level 7` 更细的收缩，需要更窄窗口或更高精度。
- 当前代码与产物：
  - 代码：`src/susy_mp_bootstrap/figure6_cubic.py`
  - 测试：`tests/test_figure6_cubic.py`
  - 输出：
    - `runs/figure6_level7/`
    - `runs/figure6_hierarchy_eta_levels7_to_10_sparse/`
  - 理论输入：
    - `notes_pro/Figure6-guidance-1.md`
    - `notes_pro/Figure6-guidance-2.md`
    - `notes_pro/Figure6-support.md`

## 总体约定

- 仓库：`recover-bootstrap-result`
- 分工：
  - `[Pro]` 理论推导、归一化、benchmark 真值
  - `[Codex]` recursion/reduction、PSD 组装、数值扫描、测试、导出
- 主线：
  - `1D analytic SUSY QM -> 1D bootstrap -> N=1 MP -> matrix quadratic -> matrix cubic`
- 项目独立于 BFSS；不复用 BFSS 代码或结果。

## M1：1D analytic kernel

- 状态：已完成
- 内容：
  - 实现 polynomial superpotential、zero-mode normalizability、一般 1D recursion、SHO specialization、最小 `⟨x^n⟩` PSD engine。
- 理论输入：
  - `notes_pro/phase_1.md`
- 代码：
  - `src/susy_mp_bootstrap/models_1d.py`
  - `src/susy_mp_bootstrap/moments_1d.py`
  - `src/susy_mp_bootstrap/sdp_core.py`
- 输出：
  - `runs/m1/`

## Matrix warmup scaffold

- 状态：已完成基础脚手架
- 内容：
  - traced-word metadata、quadratic warmup inequality、large-`N` harmonic oscillator benchmarks、cubic level-8 block counting。
- 代码：
  - `src/susy_mp_bootstrap/matrix_words.py`
  - `src/susy_mp_bootstrap/matrix_bootstrap.py`

## Figure 1：quadratic toy bootstrap

- 状态：已完成
- 方法：
  - quadratic recursion + Hankel PSD。
  - 约束版本 `u = E + 1/2` 用逐级单调剪枝。
- 结果：
  - 二维区域图完成，`K=4,5,6,7` 与 `K=4..20` 扫描均完成。
  - 一维约束线扫描完成；`400` 点粗网格上，`K=18` 存活 `10` 点，`K=19` 存活 `3` 点，`K=20` 清空。
- 代码：
  - `src/susy_mp_bootstrap/figure1_quadratic.py`
- 输出：
  - `runs/figure1_quadratic/`
  - `runs/figure1_quadratic_k4_to_20/`
  - `runs/figure1_line_u_eq_E_plus_half_k4_to_20_grid400/`

## Figure 2：quartic correction

- 状态：已完成
- 方法：
  - 直接 forward recursion。
  - 左图做 `E` vs `⟨x⟩` 投影，右图做 `E` vs `⟨x^2⟩` 投影。
- 结果：
  - `K=5,6,7,8` 两张图都已复现出层级收缩。
- 代码：
  - `src/susy_mp_bootstrap/figure2_quartic.py`
- 输出：
  - `runs/figure2_ex_k5_to_8/`
  - `runs/figure2_eu_k5_to_8/`

## Figure 3：quartic correction convex bootstrap

- 状态：已完成
- 方法：
  - `x/p` convex block matrix bootstrap。
  - 后续切到 reduced shifted-scaled formulation，去掉 duplicate identity，改成 continuation + explicit certification。
- 主要困难：
  - 高 level 原始 formulation 病态；`optimal_inaccurate` 很多。
- 解决：
  - reduced basis
  - shifted-scaled variables
  - pure-`p` constraints 增强
  - residual / PSD / nesting 显式验收
- 代码：
  - `src/susy_mp_bootstrap/figure3_convex.py`
- 输出：
  - `runs/figure3_convex_monotone_k2_to_7/`
  - `runs/figure3_certified_k5_k6/`
  - `runs/figure3_shifted_scaled_k2_to_6/`

## Figure 4：cubic SUSY QM ground-state energy

- 状态：已完成
- 方法：
  - shifted-scaled cubic reducer
  - ordinary bootstrap lower bound + ground-state matrix upper bound
  - high-`g` 区用 fit-guided local refinement
  - 同图加入 WKB 与 RR benchmark
- 主要困难：
  - fixed-energy feasible window 很窄，边界 refinement 很贵。
  - high-`g` 区 first-order solver 容易给小的负 slack。
- 解决：
  - canonical basis compression
  - guide-centered feasible seed
  - local fit-guided edge refinement
  - RR / WKB 作为稳定 benchmark
- 代码：
  - `src/susy_mp_bootstrap/figure4_cubic.py`
- 理论笔记：
  - `notes_pro/Figure4- WKB.md`
  - `notes_pro/Figure4- RR.md`
- 代表输出：
  - `runs/figure4_combined_sparse_low_dense_high/figure4_bounds_with_rr_visible.png`
  - `runs/figure4_highg_20_to_100_fit_guided/figure4_bounds_fullframe_with_rr_visible.png`
  - `runs/figure4_near100_refined_edges/figure4_near100_zoom.png`
  - `runs/figure4_rr_benchmarks/figure4_rr_reference_curves.png`

## Figure 5：small-`g` cubic lower bounds

- 状态：进行中；small-`g` 左边界已进一步推进
- 方法：
  - 优先按第一份指导：回到原始 `(x,p)` global reducer。
  - 数值上因 full higher-level basis 太慢，切到 compact/completion 路线。
  - `g >= 0.4`：沿用原来的 fixed-`E` feasibility + bisection。
  - `g < 0.4`：改用 instanton-rescaled `eta/rho` search。
  - `x^2` 图：仍用 lower-edge 上方小窗扫描；`nu=(m_2-1/2)/g^2` 只作为内部数值目标参考。
- 已解决困难：
  - `x^2` 最小化里无界 `margin` bug 已修复。
  - small-`g` 线性 `E` 扫描已被替换；这一步让左边界从 `g=0.4` 推进到了 `g=0.25/0.3/0.35`。
- 仍未解决：
  - `level 10` 的 small-`g` 可行性仍很差。
  - `H_nu` deformation、arbitrary-precision SDP、以及更稳的 implicit/coupled `(m_n,k_n)` formulation 还没接入。
- 代码：
  - `src/susy_mp_bootstrap/figure5_cubic_smallg.py`
- 理论笔记：
  - `notes_pro/Figure5- guidance-1.md`
  - `notes_pro/Figure5- guidance-2.md`
  - `notes_pro/Figure5- small-g-guidance-3.md`
  - `notes_pro/Figure5- small-g-guidance-4.md`
- 当前输出：
  - `runs/figure5_smallg_k7_to_10_sparse_v2/figure5_bounds.png`
  - `runs/figure5_smallg_k7_to_10_sparse_v2/bounds.csv`
  - `runs/figure5_smallg_hybrid_eta_sparse_merge/figure5_bounds.png`
  - `runs/figure5_smallg_hybrid_eta_sparse_merge/bounds.csv`
  - `runs/figure5_level7_smallg_to_zero/figure5_level7_smallg_zoom_logE.png`
  - `runs/figure5_level7_smallg_to_zero/figure5_level7_smallg_zoom_linearE.png`
  - `runs/figure5_level7_smallg_to_zero/bounds.csv`

## Figure 5：level-7 small-`g` to zero

- 状态：已完成
- 方法：
  - 仅保留 `level 7`
  - small-`g` 用 instanton-rescaled `eta/rho` search
  - 原始 `(x,p)` reducer 保持不变
  - `g=0` 点直接接 exact SUSY harmonic oscillator limit
- 结果：
  - `level 7` 已从 `g=0.25` 推进到 `g=0.022`
  - 目前 double precision 下再往左就会碰到 instanton scale underflow
  - exact endpoint 已接上：
    - `g=0`
    - `E=0`
    - `⟨x^2⟩=1/2`
- 输出：
  - `runs/figure5_level7_smallg_to_zero/figure5_level7_smallg_zoom_logE.png`
  - `runs/figure5_level7_smallg_to_zero/figure5_level7_smallg_zoom_linearE.png`
  - `runs/figure5_level7_smallg_to_zero/bounds.csv`

## Figure 5：level-7 / level-8 near `g=0`

- 状态：已完成
- 方法：
  - 复用 `level 7` 到零点 run
  - 在同一 `g` 网格上补 `level 8`
  - 保持原始 `(x,p)` reducer 与 instanton-rescaled `eta/rho` search
  - `g=0` 直接接 exact SHO 点
- 结果：
  - `level 7` 与 `level 8` 都到 `g=0.022`
  - 当前采样点上两条 energy lower-bound 曲线重合
  - `x^2` 曲线只在右端拉开；`level 8` 在 `g=0.25` 的点仍是 widened-window 下的 `optimal_inaccurate`
- 输出：
  - `runs/figure5_level7_vs_level8_smallg_to_zero/figure5_level7_vs_level8_smallg_logE.png`
  - `runs/figure5_level7_vs_level8_smallg_to_zero/figure5_level7_vs_level8_smallg_linearE.png`
  - `runs/figure5_level7_vs_level8_smallg_to_zero/bounds.csv`
