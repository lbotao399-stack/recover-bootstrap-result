# BFSS bootstrap 日志

## 最新未解决困难（优先关注）

- Appendix B.1 的 toy `Figure 4` 已完成第一轮代数实现，但当前还没有拿到可信的 level-12 archipelago 数值图。
- 当前明确卡住的地方不在低阶公式，而在 fixed-`E` PSD 的数值稳定性：单点 slice 下，`SCS` 会给出 `optimal_inaccurate` 的可行区间，但上下界次序仍会翻转；`SDPA/CVXOPT` 在同一点又报 `infeasible`。这说明现在的 blocker 是 solver 条件数与自由基底参数化，而不是 `C/R/H/swap` 的基础约束没写出来。
- 另一个仍未解决的问题是：当前自动线性约化得到的是 `10` 维自由基底，而不是解析指导预期的仅由 `\langle x^{2k}\rangle` 组成的 `6` 维基底。现阶段代码先保留这个 overcomplete basis，以便继续验证 positivity/scan 管线。

## 2026-04-18

- 初始化完成：已在 `Bootstrapping Ground State Correlators in Matrix Theory, Part I/` 建立新工作目录与新日志。
- 论文源码已纳入仓库：使用本地 arXiv 源包 `qcohomology_project/08_references/bfss_bootstrap_part1/arxiv-2410.14647-source.tar.gz` 解出 `bootstrapBFSS.tex` 及随附图文件。
- 当前可直接使用的论文主文件：`Bootstrapping Ground State Correlators in Matrix Theory, Part I/bootstrapBFSS.tex`。
- 已归档综合指导：将 2026-04-18 的两份 BFSS 工作流长指导合并为 `notes_pro/BFSS-PartI-guidance-1.md`，作为依赖链优先的总指导。
- BFSS figure 复现尚未开始；当前仍处于规范冻结与指导归档阶段。
- 已归档 toy Figure 4 指导：将两份 Appendix B / `x2y2` 实现指导整理为 `notes_pro/BFSS-toy-Figure4-guidance-1.md`。
- toy Figure 4 现已开始复现：当前进入代码实现与低阶 regression 阶段，尚未完成最终 archipelago 作图。
- toy Figure 4 第一轮实现已落地：
  - 新增模块：`src/susy_mp_bootstrap/toy_figure4_x2y2.py`
  - 新增测试：`tests/test_toy_figure4_x2y2.py`
  - 已通过的低阶 regression：
    - `\langle p_x\rangle=\langle p_y\rangle=0`
    - `\langle x p_x\rangle=\langle y p_y\rangle=i/2`
    - `\langle p_x^2\rangle=\langle p_y^2\rangle=\langle x^2 y^2\rangle=2E/3`
  - 当前数值状态：fixed-`E` SDP 还在调稳定性，不能把单点输出直接当作 Figure 4 边界。
- toy Figure 4 已产出第一版自己的图：
  - 输出目录：`runs/toy_figure4_x2y2_scs_manual5/`
  - 图文件：`runs/toy_figure4_x2y2_scs_manual5/toy_figure4_x2y2.png`
  - 方法：level-12 moment truncation + parity/time-reversal/swap reduction + automatic nullspace reduction（优先保留 `\langle x^{2k}\rangle`）+ `SCS` fixed-`E` min/max slice
  - 当前粗网格能量点：`E = 0.5, 1.5, 2.5, 3.5, 4.5`
  - 对应区间：
    - `E=0.5`: `\langle x^2\rangle \in [0.689, 0.699]`
    - `E=1.5`: `\langle x^2\rangle \in [0.385, 4.530]`
    - `E=2.5`: `\langle x^2\rangle \in [1.441, 1.468]`
    - `E=3.5`: `\langle x^2\rangle \in [1.603, 2.567]`
    - `E=4.5`: `\langle x^2\rangle \in [1.762, 3.214]`
  - 这一版已经出现“窄岛 + 宽连通区”的 archipelago 雏形，但 solver 状态仍是 `optimal_inaccurate`，因此它现在应视为实现版 / diagnostic Figure 4，而不是 paper 级最终复现。
